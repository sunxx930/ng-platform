# NG-AI-Platform 改造清单（Roadmap）

> 排期池：上线阻塞项优先；非阻塞按用户节奏推进。逐版升级内容见 `CHANGELOG.md`。

## 当前主线：v1.2（上线后补丁/增强）
| 项 | 状态 |
|---|---|
| 升级模式 A：检查更新（当前版本 + "↻更新"按钮，拉官网 `version.json`）| ✅ 已做（v1.2 起点）|
| 数值任务自动复算比对（现人工复核为唯一防线）| ✅ 首版已做（v1.2.0：独立复算 PASS/CHECK + 审计 + 前端展示；人工仍终审）|
| 前端点击级 UI 回归（Playwright）| ✅ 已做（`tests/ui/ui_smoke.js` 顶栏/登录 + `tests/ui/review_flow.js` 完整复核流程：owner 可审、数值提示、意见门、打回/返工）|

## 升级模式 C：真自动更新（挂起，成熟后再上）
- mac 接 **Sparkle**（标准自动更新：自动下载 + 替换 + 保数据 `~/.ng-platform/`）
- Windows 接 **Squirrel.Windows** 或自建替换器
- 前提/代价：引入原生框架 → **改造 PyInstaller 打包 + Developer ID 签名/公证链路**（现稳定发版流水线要重动）；建议用户量/技术成熟、且每版都过完整签名公证后再做。
- 触发条件：A 检查更新跑顺 + 官网下载体系稳定后启动。

## 长期备选（不排期）
- CLOUD/demo 模式生产收严复核（NG_DEMO_MODE 上线确认不带）
- deliverable/version 展示优化、审计可视化
- 官网下载区每版 changelog 自动生成（CHANGELOG.md 作源）✅ 已做（`scripts/gen_changelog_html.py` → changelog.html + 官网导航"更新日志"，发版跑一次即可）
