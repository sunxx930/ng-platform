# ng-platform 三签收尾交接（2026-08-31）

> 本交接文档由 ng-platform 狗粮验证任务产出（project fe71cbe7，task 933bfd84）。
> 平台首次端到端真实跑通：项目 → 任务 → 指派/转移 → Worker 自动推进 → 产出提交 → 复核 → 完成 → 审计。

## 1. 三方会签结论

**正式生产条件性放行**（系统架构师基于 06:27–06:36 新树签核，独立复核 21/21 + Docker E2E 全链路通过）。

四项重点全部确认：
1. **strict token**：`auth.py` 抽 `resolve_tokens`，`NG_ENV=production`/`NG_STRICT_TOKENS=1` 拒不安全默认 token（缺值拒启）
2. **缺 NG_APP_PASSWORD 拒跑**：`run_migrations.py` 生产模式缺密码即退出
3. **ALTER ROLE 同步**：角色已存在也统一改密（绑定参数/转义内联，PG DDL 不吃绑定参数）
4. **DB/JSONL 幂等冲突 409**：`IdempotencyConflict`，同 key 不同意图抛 409

P1 假 200 已消除。

## 2. 非阻塞收尾（已完成）

- compose 三服务显式 `NG_ENV=production`（migrate/api/workers）
- 端口收敛：db 不发布 host 端口；api 绑 `127.0.0.1:8000`；删 obsolete `version`
- CI 复跑：`.github/workflows/e2e.yml`（待 git init 后启用）

## 3. 验证证据

- 测试 **21 全绿**（P0/P1 各新增用例）
- Docker E2E 生产 compose 下重跑全过
- 生产模式启动守卫：无 token 拒启 / 带强 token 正常

## 4. 后续阶段（README 已记）

- 复核/审批 gate 通用化（当前仅 pause_project 专用）
- 投影物化/乐观锁（projection_version / expected_version）
- 前端看板（frontend/ 待建）

## 5. 本次狗粮发现（新）

端到端循环平台侧全部走通；**agent 执行与回写是唯一未闭环的堵点**：
- 转移消息已路由给龙虾（status: read），但龙虾未回、无产出
- 平台无 agent 结果回写端点（仅 `/deliverables`）
- 建议：加 agent 结果接收通道 + 转移消费方（下一阶段实现）
