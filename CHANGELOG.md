# CHANGELOG（升级内容）

> 发布惯例（类 openclaw）：每次发版 = **版本号 + 日期 + 升级内容清单**。
> 升级内容分四类标注：**新增 / 修复 / 安全 / 其他**；用户可见项尽量口语化。
> 本文从下往上：新版本在上。

## v1.2（进行中）
**新增**
- 升级模式 A：检查更新——顶栏显示当前版本 vX.Y.Z + "↻ 更新"按钮（服务端拉官网 `version.json` 对比，有新版本提示去官网下载）；发布物自带版本号，`/health`/`/version` 返回当前版本
- 真自动更新（Sparkle/Squirrel）已列入改造清单（成熟后再上，避免动现签名链路）

## v1.1.2（2026-09-05）— 上线阻塞清零版
**修复**
- file_ref 契约回归：自动执行/人工提交的交付证据恢复（file_missing 恒真问题），E1 上游注入恢复（下游能读到上游最新材料，不再旧版污染）；兼容旧 `artifacts/` 前缀与根内绝对路径，沙箱外仍拒 403
- CI docker-e2e：前端 dist 资产纳入仓库，Docker/全新环境不再缺 assets 导致 api 起不来
- 并发复核去重：同一交付不再生成多个复核请求（幂等键绑定交付内容）
- 终态守卫：任务完成/取消/归档后不能再追加交付物
- 交付物 version 字段随返工递增（同内容重试保持幂等）

**其他**
- vite dev 默认代理端口 8001→8020
- 升级/重启操作步骤文档化

## v1.1.1（2026-09-05）— 上线前必备修复
**修复**
- 二次人工复核不再 409 死锁（复核请求幂等键加入轮次分量）
- 任务转移去重以"最近一次转移"为准（换人重转不再误判冲突）
**安全**
- 新增"悬空任务"告警：归属无执行者且 30 分钟无进展 → 审计+通知一条（不再静默挂起）

## v1.1（2026-09-05）— 上线前走查 P0 修复 + 真人复核
**安全**
- PATCH 不能再直接置任务 completed（只能复核通过/审批通过触发）
- 对象级隔离：跨账号按 project/task 读/写全部拒绝（403/404），不再只看列表层
- 交付物 file_ref 沙箱：仅限 artifacts 目录，绝对路径/目录穿越拒绝（阻断任意服务端文件读取）
**新增**
- 真人账号可被指派为 reviewer 并完成复核；项目 owner 可复核自己项目（单用户闭环）
**修复**
- 复核打回（需修改/拒绝）必须填写修改意见，防"盲改"
- 前端：复核按钮按权限显隐、打回意见输入框、登录/操作可用（dev 代理可配置）
**其他**
- Windows 版改 onefile 单文件 + 无黑窗启动；mac 签名/公证脚本健壮化（暂存目录/编码）；官网 logo 上线

## v1.0 及之前（2026-08～09 开发期，要点）
- Agent 团队操作系统：目标解析拆解、分工、执行、复核闭环、审计（多轮试用迭代）
- 9/3 批修复：返工/二次复核闭环、复核意见驱动重做、打回≥3 次保险丝、幂等落盘、转移冲突/resend、依赖链串行开工、证据字段服务端核算
- 多用户隔离（列表层）、注册/登录、事件溯源审计
- macOS 签名 + 公证分发（Developer ID、官网直发）
